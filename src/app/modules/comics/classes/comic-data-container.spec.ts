import { ComicDataContainer } from './comic-data-container';

describe('ComicDataContainer', () => {
  it('should create an instance', () => {
    expect(new ComicDataContainer()).toBeTruthy();
  });
});
