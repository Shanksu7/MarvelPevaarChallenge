import { CreatorDataWrapper } from './creator-data-wrapper';

describe('CreatorDataWrapper', () => {
  it('should create an instance', () => {
    expect(new CreatorDataWrapper()).toBeTruthy();
  });
});
