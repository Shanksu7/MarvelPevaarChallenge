import { StoryDataContainer } from './story-data-container';

describe('StoryDataContainer', () => {
  it('should create an instance', () => {
    expect(new StoryDataContainer()).toBeTruthy();
  });
});
