import { SeriesDataContainer } from './series-data-container';

describe('SeriesDataContainer', () => {
  it('should create an instance', () => {
    expect(new SeriesDataContainer()).toBeTruthy();
  });
});
