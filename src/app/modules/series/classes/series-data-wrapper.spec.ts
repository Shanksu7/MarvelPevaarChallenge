import { SeriesDataWrapper } from './series-data-wrapper';

describe('SeriesDataWrapper', () => {
  it('should create an instance', () => {
    expect(new SeriesDataWrapper()).toBeTruthy();
  });
});
