import { EventDataContainer } from './event-data-container';

describe('EventDataContainer', () => {
  it('should create an instance', () => {
    expect(new EventDataContainer()).toBeTruthy();
  });
});
