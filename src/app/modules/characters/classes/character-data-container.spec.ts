import { CharacterDataContainer } from './character-data-container';

describe('CharacterDataContainer', () => {
  it('should create an instance', () => {
    expect(new CharacterDataContainer()).toBeTruthy();
  });
});
