import { CharacterDataWrapper } from './character-data-wrapper';

describe('CharacterDataWrapper', () => {
  it('should create an instance', () => {
    expect(new CharacterDataWrapper()).toBeTruthy();
  });
});
